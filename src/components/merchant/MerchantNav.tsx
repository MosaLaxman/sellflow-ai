'use strict';
'use client';

import React from 'react';

/**
 * MerchantNav — preserved for backwards compatibility.
 * Navigation is now rendered continuously in src/app/merchant/layout.tsx
 * to ensure persistent client-side state and bidirectional gliding animations.
 */
export const MerchantNav: React.FC<{ merchantSlug?: string }> = () => {
  return null;
};
